[workspace]
authors = ["akoen <git@koen.ca>"]
channels = ["conda-forge"]
name = "simmetrix"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[pypi-dependencies]
nanobindgen = { git = "https://github.com/pycad-dev/nanobindgen" }

[dependencies]
python = "3.12.*"
