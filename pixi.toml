[workspace]
authors = ["akoen <git@koen.ca>"]
channels = ["conda-forge"]
name = "simmetrix"
platforms = ["linux-64"]
version = "0.1.0"

[tasks]

[pypi-dependencies]
nanobindgen = { git = "https://github.com/pycad-dev/nanobindgen" }
pysms = { path = ".", editable = true }

[dependencies]
python = "3.12.*"
nanobind = ">=2.9.2,<3"
