"""Compiled C-extension package marker.

The contents of this directory (other than this file) are produced by the CMake
build: `_core.cpython-*.so`, `libnanobind.so`, and the generated `_core.pyi`
stub. This `__init__.py` is intentionally minimal; it exists so that
`phnx._core` is a regular package both in the source tree (for the
scikit-build-core editable install MetaPathFinder, which only registers
files it knows about) and in the installed wheel.
"""
